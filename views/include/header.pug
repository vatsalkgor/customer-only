html
  body
    .loader
    style(type="text/css").
      .loader {
      position: absolute;
      top:50%;
      left:50%;
      border: 16px solid #f3f3f3; /* Light grey */
      border-top: 16px solid #3498db; /* Blue */
      border-radius: 50%;
      width: 120px;
      height: 120px;
      animation: spin 2s linear infinite;
      background: rgba(189, 195, 199, 1);
      z-index: 10;
      }
      @keyframes spin {
      0% { transform: rotate(0deg); }
      100% { transform: rotate(360deg); }
      }
    nav.navbar.navbar-expand-sm.bg-dark.navbar-dark
      // Brand
      a.navbar-brand(href="/Dashboard") HOME
      // Toogler
      button.navbar-toggler(type="button" data-toggle="collapse" data-target="#collapsibleNavbar")
        span.navbar-toggler-icon
      // Links
      #collapsibleNavbar.collapse.navbar-collapse
        ul.navbar-nav
          li.nav-item.dropdown
            a#navbardrop.nav-link.dropdown-toggle(href="#" data-toggle="dropdown")
              | Addition
            .dropdown-menu
              a.dropdown-item(href="/Addition/Party") Party
              a.dropdown-item(href="/Addition/Group") Item Group
              a.dropdown-item(href="/Addition/Item") Item Name
          li.nav-item.dropdown
            a#navbardrop.nav-link.dropdown-toggle(href="#" data-toggle="dropdown")
              | Voucher
            .dropdown-menu
              a.dropdown-item(href="new_sell.php") Sell
              a.dropdown-item(href="new_payment.php") Payment Entry
          // Dropdown
          li.nav-item.dropdown
            a#navbardrop.nav-link.dropdown-toggle(href="#" data-toggle="dropdown")
              | Reports
            .dropdown-menu
              a.dropdown-item(href="customer_bill_report.php") Customer Bill Reports
              a.dropdown-item(href="daily_report.php") Daily Reports
              a.dropdown-item(href="person_wise_report.php") Person Wise Reports
              a.dropdown-item(href="account_info.php") Account Information
          li.nav-item.pull-right
            a.nav-link(href='/Logout') Logout